---
title: YaYaw Table Documentation
description: Open-source React data table for Shadcn UI and TanStack Table with URL state, Server Actions, bulk actions, and full code ownership
---

# YaYaw Table Documentation

YaYaw Table is a flexible data table component library for React. **One component, clean API:** pass `getTableConfig` and `getTableActions`, and the table handles URL state (Nuqs), server-side data, and CRUD/bulk actions.

## 🚀 Quick Start

Install via the [Shadcn registry](/docs/installation), then wire config and actions to `DataTable`:

```tsx
import { DataTable } from "@/components/ui/yayaw-table";
import { getTableConfig, getTableActions } from "./table-config";

export const MyTable = () => (
  <DataTable
    tableType="products"
    getTableConfig={getTableConfig}
    getTableActions={getTableActions}
    title="Products"
  />
);
```

You need at least **getTableConfig** (columns, table options) and **getTableActions** (e.g. **list** for data). For Next.js with URL state, add [NuqsAdapter](/docs/url-state) to your layout. See [Provider & Setup](/docs/setup) for the full picture.

## ✅ Benefits

- **Less boilerplate** – Clear, focused API
- **Minimal setup** – Sensible defaults
- **Stable** – No hydration loops with Next.js
- **Powered by TanStack Table** – Great performance
- **Built-in grouping** – Expand/collapse support
- **Type-safe** – First-class TypeScript types
- **SSR-friendly** – Works with Next.js 15

## 🔧 Features

### Grouping
Grouping, pagination, and sorting are enabled via table configuration and column types. No heavy wiring needed.

### Saved views
Users can save and restore URL-backed views that include filters, search, sorting, column layout, display mode, Kanban settings, Gallery settings, grouping, pinning, and page size.

### Display modes
Use the default table view, a Kibo UI-powered Kanban board, or a responsive Gallery card grid from the same data source. Kanban and Gallery keep selection, row actions, row click behavior, filters, sorting, pagination, and saved views aligned with table mode.

### Pagination
Accessible pagination controls with proper ARIA and keyboard navigation.

### Sorting
Click headers to sort. Enable/disable per column.

### Column types
```tsx
const columns = [
  { id: 'name', type: 'text', header: 'Name' },
  { id: 'brand', type: 'tag', header: 'Brand' },
  { id: 'price', type: 'number', header: 'Price' },
  { id: 'metadata', type: 'json', header: 'Metadata' },
  { id: 'token', type: 'string', header: 'Token', showQuotes: true },
  { id: 'active', type: 'boolean', header: 'Active' },
  { id: 'date', type: 'date', header: 'Created' },
];
```

### Toolbar & Menus
Everything is available from the table toolbar:

- Sorting via column headers
- Column filters and an advanced filters panel
- Grouping toggles when supported by column types
- Column visibility menu (show/hide, reset order)
- Saved views and a display switcher for table, Kanban, and Gallery modes
- Global search input
- CSV export from toolbar (all filtered rows)
- Compact toolbar actions mode (icon + tooltip) via `table.actionsAsIcons`
- Layout presets for admin, catalog, and preview-driven tables
- Active-row and row activation primitives for master-detail layouts
- Bulk actions menu (Edit, Copy, Export, Delete)
- Auto-generated edit forms for row/item editing

## 📖 Documentation

- [Installation](/docs/installation) – Shadcn registry install and project setup
- [Provider & Setup](/docs/setup) – getTableConfig, getTableActions, getFormConfig, DataTable props
- [Forms and Collection Fields](/docs/forms) – Built-in form fields, native array editors, nested collections, and validation
- [DataTable](/docs/datatable) – Component props reference
- [Configuration](/docs/configuration) – Table and column options
- [Columns](/docs/columns) – Column types and definitions
- [Actions](/docs/actions) – Actions API shape (list, create, update, delete, bulk)
- [Query Integration](/docs/query-integration) – Shared QueryClient architecture and invalidation
- [Bulk Actions](/docs/bulk-actions) – Confirmation flow and callback contract
- [Server-side & Server Actions](/docs/server-actions) – Next.js Server Actions for list and CRUD
- [URL state (Nuqs)](/docs/url-state) – Sort, filters, display modes, saved views, and pagination in the URL
- [Troubleshooting](/docs/troubleshooting) – Fix common runtime issues
- [Translations](/docs/translations) – Override UI strings

## 🎨 Customization

The table uses Tailwind by default. You can customize classes via standard `className` props.

## 🔗 Example

See **/example** in this repo for a full UI integration example: URL state (Nuqs), saved views, table/Kanban/Gallery display modes, advanced filters, bulk edit, and locally persisted demo data (`localStorage` + dataset reset).

For the server-driven pattern, see [Server-side & Server Actions](/docs/server-actions).

## 🙏 Acknowledgements

YaYaw Table stands on the shoulders of excellent open-source projects. Huge thanks to their maintainers and contributors:

- [TanStack Table](https://tanstack.com/table) - core table engine and performance foundations
- [TanStack Query](https://tanstack.com/query) - async data-fetching and cache model
- [shadcn/ui](https://ui.shadcn.com) - registry and UI primitives integration model
- [Nuqs](https://nuqs.io) - URL state management patterns
- [Jotai](https://jotai.org) - lightweight atomic state for internal table/UI state
- [dnd-kit](https://dndkit.com) - drag-and-drop behavior used in interactive table features
- [Floating UI](https://floating-ui.com) and [Base UI](https://base-ui.com) - robust positioning and accessible headless primitives
