---
"yayaw-table-workspace": patch
---

Apply React advanced filters to the table instance ID instead of the configuration catalogue key. Tables with distinct `tableId` and `tableType` now filter their own data and saved views, with URL synchronization enabled or disabled.
