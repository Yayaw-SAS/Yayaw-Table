---
"yayaw-table-workspace": minor
---

Add independent host-resolved `canEdit` and `canDelete` permissions on saved views in React and Vue. Shared read-only views remain selectable, copyable and eligible as personal favorites. Hosts must continue enforcing authorization in their persistence actions.
