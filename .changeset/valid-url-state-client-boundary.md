---
"yayaw-table-workspace": patch
---

Keep the React URL-state hook's client directive before its Gantt imports so copied registry files compile in Next.js. Vue's equivalent URL-state behavior is unchanged.
