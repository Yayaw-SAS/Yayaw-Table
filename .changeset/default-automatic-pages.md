---
"yayaw-table-workspace": minor
---

Add `table.defaultAutoPageSize` to start table views in automatic viewport pagination in React and Vue while preserving manual numeric choices within the active view.
