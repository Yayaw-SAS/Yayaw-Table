---
"yayaw-table-workspace": minor
---

Add an optional filter bar for catalogue option and boolean columns in React and Vue. The bar uses native column-filter state, searchable multi-selection with filter icons, and the same controls in Options. Showing or hiding the bar preserves filters and saved views.
