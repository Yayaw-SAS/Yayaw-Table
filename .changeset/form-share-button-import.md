---
"yayaw-table-workspace": patch
---

Form sharing imports `buttonVariants` from the shadcn `button` component instead of a file that only exists in this repository, so installs of the registry item compile.
