---
"yayaw-table-workspace": patch
---

Keep automatic pagination on the selected page when variable column widths change header wrapping. Header/footer measurements affect fit without raising the viewport capacity ceiling in React and Vue.
