---
"yayaw-table-workspace": minor
---

Add `onOpenDetails` in React and Vue so the native View action and row activation can open an application-owned record route or drawer. The callback takes precedence over built-in details and also enables View for read-only tables.
