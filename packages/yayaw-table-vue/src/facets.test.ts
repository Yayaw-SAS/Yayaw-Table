import { it } from "vitest";
import { facetsSuite } from "../../../tests/facets-suite";
import {
  facetBlockColumn,
  facetBlockEntries,
  facetBlockProblems,
  facetBlockProps,
  facetBlockSchema,
  loadFacetBlockCounts,
  toggleFacetBlockValue,
} from "./dashboard/dashboard-facets";
import {
  checkDashboardFilterValue,
  dashboardSourceFilterRules,
} from "./dashboard/dashboard-model";
import {
  canToggleFacet,
  clearFacets,
  countFacetValues,
  FACET_EMPTY_KEY,
  facetAggregateParams,
  facetCountParams,
  facetEntries,
  facetKeyTarget,
  facetLabel,
  facetSelection,
  facetsShownIn,
  loadFacetCounts,
  normalizeFacetsConfig,
  resolveFacets,
  selectedFacetCount,
  toggleFacetValue,
  visibleFacetEntries,
} from "./facets-model";
import {
  buildFolderDirectory,
  canCreateFolderUnder,
  createFolderRecord,
  defaultNewFolderParent,
  folderChoiceComplete,
  folderChoiceOf,
  folderChoiceText,
  folderFacetLabel,
  folderLocationText,
  folderPathText,
  folderTableOptions,
  folderTreeOf,
  loadFolderDirectory,
  newFolderName,
  newFolderShownIn,
  searchFolders,
  toggleFolderChoice,
} from "./folder-directory";

facetsSuite(it, {
  facets: {
    FACET_EMPTY_KEY,
    canToggleFacet,
    clearFacets,
    countFacetValues,
    facetAggregateParams,
    facetCountParams,
    facetEntries,
    facetKeyTarget,
    facetLabel,
    facetSelection,
    facetsShownIn,
    loadFacetCounts,
    normalizeFacetsConfig,
    resolveFacets,
    selectedFacetCount,
    toggleFacetValue,
    visibleFacetEntries,
  },
  folders: {
    buildFolderDirectory,
    canCreateFolderUnder,
    createFolderRecord,
    defaultNewFolderParent,
    folderChoiceComplete,
    folderChoiceOf,
    folderChoiceText,
    folderFacetLabel,
    folderLocationText,
    folderPathText,
    folderTableOptions,
    folderTreeOf,
    loadFolderDirectory,
    newFolderName,
    newFolderShownIn,
    searchFolders,
    toggleFolderChoice,
  },
  dashboard: { checkDashboardFilterValue, dashboardSourceFilterRules },
  block: {
    facetBlockColumn,
    facetBlockEntries,
    facetBlockProblems,
    facetBlockProps,
    facetBlockSchema,
    loadFacetBlockCounts,
    toggleFacetBlockValue,
  },
});
