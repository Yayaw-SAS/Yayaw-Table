import { it } from "vitest";
import { formI18nSuite } from "../../../tests/form-i18n-suite";
import {
  formLanguageName,
  formLocaleMatch,
  formLocaleTag,
  formTextIn,
  formTextMissing,
  formTextVersion,
  normalizeFormText,
  resolveFormText,
  setFormText,
} from "./form-text";
import {
  acceptFormHiddenFields,
  acceptPublicFormResponse,
  addFormConsent,
  addFormHiddenField,
  buildPublicFormSnapshot,
  collectFormHiddenFields,
  evaluateFormView,
  formAddableLocales,
  formConsentParts,
  formHiddenFieldColumns,
  formItemMissingTranslation,
  formLabel,
  formLocales,
  formReviewConsents,
  formSettingsRows,
  formStepOptional,
  formStepPlan,
  formSteps,
  formSubmission,
  formViewLocales,
  initialFormDraft,
  moveFormQuestion,
  normalizeFormViewConfig,
  publicFormSnapshot,
  resolveFormSettings,
  toggleFormQuestion,
  updateFormHiddenField,
  updateFormQuestion,
  validateFormConsents,
  withFormServerContext,
} from "./form-view";

formI18nSuite(
  it,
  {
    acceptFormHiddenFields,
    acceptPublicFormResponse,
    addFormConsent,
    addFormHiddenField,
    buildPublicFormSnapshot,
    collectFormHiddenFields,
    evaluateFormView,
    formAddableLocales,
    formConsentParts,
    formHiddenFieldColumns,
    formItemMissingTranslation,
    formLabel,
    formLocales,
    formReviewConsents,
    formSettingsRows,
    formStepOptional,
    formStepPlan,
    formSteps,
    formSubmission,
    formViewLocales,
    initialFormDraft,
    moveFormQuestion,
    normalizeFormViewConfig,
    publicFormSnapshot,
    resolveFormSettings,
    toggleFormQuestion,
    updateFormHiddenField,
    updateFormQuestion,
    validateFormConsents,
    withFormServerContext,
  },
  {
    formLanguageName,
    formLocaleMatch,
    formLocaleTag,
    formTextIn,
    formTextMissing,
    formTextVersion,
    normalizeFormText,
    resolveFormText,
    setFormText,
  }
);
