import { it } from "vitest";
import { mapModelSuite } from "../../../tests/map-model-suite";
import { normalizeSyncValue, placeText } from "./connectors/sync-engine";
import { isTableDisplayMode, normalizeModeConfig } from "./display-modes";
import { exportMatrix } from "./export-model";
import { formColumnEditor } from "./form-view";
import { coerceImportValue } from "./import-model";
import {
  addressesToGeocode,
  boundsContain,
  boundsOfPoints,
  createGeocodeSearch,
  distanceKm,
  formatCoordinates,
  formatLocation,
  formatLocationFilterValue,
  geocodeAddresses,
  hasLocation,
  locationDetail,
  locationDraftFrom,
  locationFilterHasValue,
  locationFromDraft,
  locationFromGeocode,
  locationLabel,
  locationsEqual,
  locationToText,
  matchesLocationFilter,
  normalizeBounds,
  normalizeGeocodeResults,
  parseLocation,
} from "./location-model";
import {
  attachMapItems,
  boundsFromMap,
  clusterSize,
  expandMapCluster,
  isMapBackgroundClick,
  loadMapRows,
  MAP_CLUSTER_MAX_ZOOM,
  MAP_CLUSTER_RADIUS,
  MAP_SOURCE_ID,
  mapBboxScope,
  mapColorColumns,
  mapFeatureCollection,
  mapItemsFromFeatures,
  mapItemsWithoutClusters,
  mapLabel,
  mapLocationColumns,
  mapMarkers,
  mapNotices,
  mapPopupProperties,
  mapSettingFields,
  mapStyleChoices,
  mapWorkerUrl,
  markerColor,
  markersBounds,
  markersInBounds,
  normalizeMapViewConfig,
  rememberMapViewport,
  resolveMapSettings,
  resolveMapStyle,
  sameBounds,
  withoutLocationLabel,
} from "./map-model";
import { loadScopedRows, rowInScope } from "./scoped-rows";
import {
  dataTypeValueError,
  matchesContractFilter,
  resolveDataTypeEditor,
  TABLE_DATA_TYPES,
} from "./table-contracts";

mapModelSuite(it, {
  location: {
    addressesToGeocode,
    boundsContain,
    boundsOfPoints,
    createGeocodeSearch,
    distanceKm,
    formatCoordinates,
    formatLocation,
    formatLocationFilterValue,
    geocodeAddresses,
    hasLocation,
    locationDetail,
    locationDraftFrom,
    locationFilterHasValue,
    locationFromDraft,
    locationFromGeocode,
    locationLabel,
    locationToText,
    locationsEqual,
    matchesLocationFilter,
    normalizeBounds,
    normalizeGeocodeResults,
    parseLocation,
  },
  map: {
    MAP_CLUSTER_MAX_ZOOM,
    MAP_CLUSTER_RADIUS,
    MAP_SOURCE_ID,
    attachMapItems,
    boundsFromMap,
    clusterSize,
    expandMapCluster,
    isMapBackgroundClick,
    loadMapRows,
    mapBboxScope,
    mapColorColumns,
    mapFeatureCollection,
    mapItemsFromFeatures,
    mapItemsWithoutClusters,
    mapLabel,
    mapLocationColumns,
    mapMarkers,
    mapNotices,
    mapPopupProperties,
    mapSettingFields,
    mapStyleChoices,
    mapWorkerUrl,
    markerColor,
    markersBounds,
    markersInBounds,
    normalizeMapViewConfig,
    rememberMapViewport,
    resolveMapSettings,
    resolveMapStyle,
    sameBounds,
    withoutLocationLabel,
  },
  scoped: { loadScopedRows, rowInScope },
  contracts: {
    dataTypeValueError,
    matchesContractFilter,
    resolveDataTypeEditor,
    TABLE_DATA_TYPES,
  },
  importModel: { coerceImportValue },
  exportModel: { exportMatrix },
  formView: { formColumnEditor },
  sync: { normalizeSyncValue, placeText },
  modes: { isTableDisplayMode, normalizeModeConfig },
});
