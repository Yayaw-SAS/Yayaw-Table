# yayaw-table (package)

Build and publish from this directory in the workspace:

- Install deps at root: `bun install`
- Build package: `npm run build -w packages/yayaw-table`
- Publish: `npm publish --access public -w packages/yayaw-table`
